#pragma once

namespace logger {
    /**
     * @brief Initialize the logger (stub implementation)
     */
    void initialize();
    
    /**
     * @brief Log a message (stub implementation)
     * @param message The message to log
     */
    void log(const char* message);
    
    /**
     * @brief Cleanup the logger (stub implementation)
     */
    void cleanup();
}
